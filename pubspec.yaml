name: app_name
description: Who Used My Phone Mobile App.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  getwidget: ^5.0.0
  provider: any
  shared_preferences: ^2.2.1

dev_dependencies:
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  test: ^1.24.1
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
